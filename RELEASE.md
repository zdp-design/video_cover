# 版本说明 v1.0.0

## 发布概述

短视频封面制作工具 v1.0.0 是一个离线优先的封面设计工具，专为电商从业者和店铺评价者打造。目标：5 分钟内完成一张可发布的封面。

## 核心功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 画布尺寸预设 | ✅ | 1080x1920、1080x1440、1080x1080 |
| 文本元素 | ✅ | 支持拖拽、缩放、旋转、描边、阴影、字间距 |
| 贴纸素材 | ✅ | 6 款内置 SVG 贴纸，100% 离线可用 |
| 图形元素 | ✅ | 矩形、圆角矩形、圆形 |
| 层级调整 | ✅ | 置顶、上移、下移、置底 |
| 撤销/重做 | ✅ | 最多 50 步历史记录 |
| 模板系统 | ✅ | 3 套内置模板 + 自定义模板保存 |
| 主题配色 | ✅ | 5 套主题预设 + 一键替换 |
| 导出 | ✅ | PNG/JPEG 标准(1x)/高清(2x) |
| 本地存储 | ✅ | IndexedDB 自动保存草稿 |
| 异常恢复 | ✅ | ErrorBoundary + 错误友好提示 |
| 性能优化 | ✅ | 30 FPS 拖拽体验 |

## 技术栈

| 组件 | 版本 |
|------|------|
| React | 19.x |
| TypeScript | 5.x |
| Vite | 5.x/8.x |
| Konva | 9.x (react-konva 18.x) |
| Zustand | 5.x |
| Ant Design | 5.x |
| IndexedDB (idb) | 8.x |
| Vitest | 2.x |
| Playwright | 1.x |

## 发布前检查清单

### 自动化测试

- [x] ESLint 检查通过（`npm run lint`）
- [x] Prettier 格式化通过（`npm run format`）
- [x] 单元测试全部通过（`npm test`）
- [x] E2E 测试全部通过（`npm run e2e`）

### 手工抽检

- [ ] **带货模板**：套用模板 → 添加文本 → 调整样式 → 导出
- [ ] **探店模板**：套用模板 → 添加贴纸 → 调整位置 → 导出
- [ ] **自定义画布**：空白画布 → 添加多种元素 → 撤销重做 → 保存为模板 → 导出

### 浏览器兼容性

| 浏览器 | 支持版本 | 备注 |
|--------|----------|------|
| Chrome | 90+ | 推荐 |
| Firefox | 90+ | 支持 |
| Safari | 15+ | 支持 |
| Edge | 90+ | Chromium 内核，支持 |

> **注意**：IE 11 不支持。

### 导出质量抽检

- [ ] PNG 标准模式 (1x) 导出正常
- [ ] PNG 高清模式 (2x) 导出正常
- [ ] JPEG 导出正常，文件体积合理
- [ ] 导出图片中不包含选中框和操作手柄
- [ ] 导出图片尺寸与标注尺寸一致

### 版权与合规

- [ ] 所有内置字体使用系统字体回退链，无第三方字体版权风险
- [ ] 所有贴纸 SVG 为原创设计，无第三方版权问题
- [ ] 使用 DOMPurify 进行 SVG 净化，防止 XSS 攻击

## 已知限制

1. **历史记录不跨刷新恢复**：页面刷新后撤销/重做栈清零，但草稿会自动恢复。
2. **IndexedDB 隐私模式**：在部分浏览器的隐私模式下，IndexedDB 可能不可用，草稿不会自动保存但编辑功能正常。
3. **字体渲染差异**：不同操作系统/浏览器对同一字体的渲染效果可能有细微差异。
4. **大型画布性能**：当画布元素超过 50 个时，极端情况下可能影响拖拽流畅度。

## 数据存储

| 数据类型 | 存储方式 | 键 |
|----------|----------|-----|
| 草稿快照 | IndexedDB | `video-cover-db` / `drafts` / `current-draft` |
| 自定义模板 | IndexedDB | `video-cover-db` / `custom-templates` |
| 画布尺寸偏好 | localStorage | `video-cover:prefs:lastSize` |

## 版本历史

- **v1.0.0** (2026-06-04): 初始发布，包含完整编辑能力和导出功能

## 获取帮助

如遇问题，请检查：
1. 浏览器控制台是否有错误信息
2. 是否使用了支持的浏览器版本
3. 是否处于隐私浏览模式（可能导致草稿无法保存）

---

**下一步计划**：持续收集用户反馈，优化性能和用户体验。